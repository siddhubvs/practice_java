public static boolean isPalindrome(int number){
        int t=number;
        int rem,rev=0;
        while (number>0)
        {
            rem=number%10;
            rev=rev*10+rem;
            number=number/10;
        }
        if(rev==t) {
            return true;
        }
        return false;
    }
